// version 1.52 (changed after 1.6 committed, so actually 1.62) quickfix

#include <stdio.h>
#include <math.h>

void main() {
	printf("hello, asshole");	// fixing a little error, but not changing too much
	newFun();
	printf("Sinus(pi)=", sin(PI));
}

void newFun() {
	x+= 7;
}