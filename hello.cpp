// version 1.62 BETA (including 1.51 and 1.52 quickfix)

#include <stdio.h>
#include <math.h>

void main() {
	printf("hello, asshole");	// fixing a little error, but not changing too much
	newFun();
	printf("Sinus(pi)=", sin(PI));
	calculate_more_stuff();
	use_new_alghorithms();
	change_program_structure_a_lot();
}

void newFun() {
	x+= 7;
}
